# -------------------------------------------------------------------------
# 빌드 결과물 및 중간 파일 (GCC Artifacts)
# -------------------------------------------------------------------------

# 컴파일된 오브젝트 파일
*.o
*.lo
*.gcno   # GCC Coverage: .gcno (Note File)
*.gcda   # GCC Coverage: .gcda (Data File)

# GCC에서 생성되는 디버깅 정보 (Symbolic links)
# .d는 헤더 파일 의존성 추적에 사용되므로, makefile/자동화 툴 사용 시 제외 권장
*.d

# 최종 실행 파일 (GNU/Linux, macOS 등)
*.out
/bin/*

# 라이브러리 파일
*.a      # 정적 라이브러리 (프로젝트 빌드 결과물)
*.so     # 동적 라이브러리 (프로젝트 빌드 결과물)
*.dll    # Windows용 동적 라이브러리

# -------------------------------------------------------------------------
# 빌드 디렉토리 및 임시 폴더
# -------------------------------------------------------------------------

# 아웃 오브 소스 빌드 디렉토리 전체를 무시
/build/

# -------------------------------------------------------------------------
# 표준 빌드 시스템 관련 파일 (Makefile, CMake 사용 시)
# -------------------------------------------------------------------------

# Makefiles에서 생성되는 자동 백업/임시 파일
*~

# CMake 관련 (만약 CMake를 사용한다면)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# -------------------------------------------------------------------------
# IDE/OS 관련 파일 (운영체제 및 편집기 임시 파일)
# -------------------------------------------------------------------------

# macOS
.DS_Store

# Windows
Thumbs.db

# VS Code (예시)
.vscode/

# IDE 프로젝트/워크스페이스 파일 (예시)
*.user
*.suo